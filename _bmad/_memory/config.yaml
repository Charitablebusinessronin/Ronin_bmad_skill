# _MEMORY Module Configuration
# Generated by BMAD installer
# Version: 6.0.3
# Date: 2026-02-27T03:29:58.337Z


# Core Configuration Values
user_name: Ronin
communication_language: english
document_output_language: english chill relaxed
output_folder: "{project-root}/_bmad-output"

# Agent Memory Sidecars
sidecars:
  sarah-boone:
    path: "{project-root}/_bmad/_memory/sarah-boone-sidecar"
    files:
      - identity.md
      - collaboration-memory.md
      - learned-patterns.md
    neo4j_enabled: true
    neo4j_node: "Sarah Boone"
    neo4j_group_id: "bmad-core"

# Neo4j Connection (loaded from .env)
neo4j:
  # Default memory target (Aura). Local bootstrap scripts may intentionally
  # use a local bolt endpoint through MCP server runtime config.
  uri: "neo4j+s://e3953f5b.databases.neo4j.io"
  user_env: "NEO4J_USER"
  # Canonical secret key for BMAD memory loaders.
  # Keep docs and MCP instructions aligned to this key.
  password_env: "NEO4J_ADMIN"
